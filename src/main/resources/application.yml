spring:
  datasource:
    url: jdbc:mysql://localhost:3306/pmt_new?serverTimezone=GMT%2B8
    username: root
    password: ""
    driver-class-name: com.mysql.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
    show-sql: true

  redis:
    host: 127.0.0.1
    port: 6379
    password: lkltest#123
    database: 1
    jedis:
      pool:
        max-active: 8
        max-wait: -1
        min-idle: 0
    timeout: 10000

server:
  port: 7071
